# В стандартной библиотеке Python уже есть ряд модулей,
# которые сделают за тебя половину работы. К примеру,
# скопировать файл с помощью функции из модуля shutil можно в одну строку.

import shutil
shutil.copy('myfile', 'myfile.bak')
exit()
